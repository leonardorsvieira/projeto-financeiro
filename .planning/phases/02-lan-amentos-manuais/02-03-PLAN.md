---
phase: 02-lan-amentos-manuais
plan: 03
type: execute
wave: 2
depends_on: [02-01, 02-02]
files_modified: []
autonomous: true
requirements: [DSP-01, DSP-02, DSP-03]
must_haves:
  truths:
    - "Usuário logado registra, edita e exclui lançamentos na web (S5 lista + S6 form)"
    - "Lista vinda da nuvem com sync realtime entre sessões; validação de campos no form"
    - "Deploy atualizado em GH Pages e testes widget verdes"
  artifacts:
    - path: "app/lib/features/lancamentos/presentation/lancamentos_list_screen.dart"
      provides: "Tela S5: lista realtime, FAB, menu excluir com confirmação, empty state"
      min_lines: 80
    - path: "app/lib/features/lancamentos/presentation/lancamento_form_screen.dart"
      provides: "Tela S6: form novo/editar com validação (descricao, valor BRL, categoria, forma, data, vencimento)"
      min_lines: 80
    - path: "app/test/features/lancamentos/presentation/lancamentos_flow_test.dart"
      provides: "Testes widget: lista, criação, edição, exclusão com fake repo"
      min_lines: 60
---

<objective>
Construir as telas S5 (lista) e S6 (formulário) da Fase 2 — registro, edição e exclusão de lançamentos com validação e sync — integrar nas rotas, cobrir com testes widget e deployar no GH Pages.

Purpose: Entregar DSP-01/02/03 na web com provas automatizadas e verificação manual.
Output: Home real com CRUD de lançamentos sincronizado; testes green; deploy atualizado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-lan-amentos-manuais/02-CONTEXT.md
@.planning/phases/02-lan-amentos-manuais/02-SKELETON.md
@.planning/phases/02-lan-amentos-manuais/02-UI-SPEC.md
@.planning/phases/02-lan-amentos-manuais/02-VALIDATION.md
</context>

<threat_model>
Threats addressed by this plan:
- T-04-02: Payload malicioso/validação client → form valida descricao obrigatória, valor>0 com 2 casas; mensagens inline; botão salvar desabilado durante saving.
- T-04-04: Formatação monetária → exibição sempre via converter (cents→BRL); inputs editados como texto e parseado por parseValorBRLParaCentavos.
- T-04-01: Área sensível → telas só acessíveis logado (router redirection existente da Fase 1); exlusão com confirmação (evita delete acidental — disponibilidade/dados).
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Telas S5 (lista) e S6 (form), integração no router</name>
  <files>app/lib/features/lancamentos/presentation/lancamentos_list_screen.dart, app/lib/features/lancamentos/presentation/lancamento_form_screen.dart, app/lib/features/lancamentos/presentation/lancamento_form_validators.dart, app/lib/router/app_router.dart, app/lib/features/home/presentation/home_screen.dart, app/lib/main.dart</files>
  <read_first>.planning/phases/02-lan-amentos-manuais/02-UI-SPEC.md, app/lib/features/auth/presentation/auth_screen.dart (padrões de form), app/lib/features/home/presentation/home_screen.dart, app/lib/router/app_router.dart</read_first>
  <action>
  1. `lancamentos_list_screen.dart` (S5): consome `lancamentosStreamProvider` com `ref.watch` + `when` (loading/error/data). `data` → `ListView.builder` de tiles Material 3 (title descricao, subtitle "dd/MM/yyyy · categoria", trailing o valor BRL em `colorScheme.error`, leading chip/avatar de categoria). RefreshIndicator p/ force refetch. Empty state com texto da UI-SPEC. FAB "+" → `context.push('/lancamentos/novo')`. Menu ⋮ por item: Editar (`/lancamentos/:id`) e Excluir → AlertDialog de confirmação → `lancamentosRepository.delete(id)`; snackbar "Lançamento excluído" / erro "Não foi possível excluir". Erro do stream: message + "Tentar de novo".
  2. `lancamento_form_screen.dart` (S6): `LancamentoFormScreen({lancamento?})` (modo editar se não-nulo, senão `:id` do path). `Form` + campos (descricao, valor TextFormField com prefixo "R$", categoria DropdownButtonFormField, forma DropdownButtonFormField, data + vencimento via `showDatePicker`, obs multiline). Salvar → validação (`lancamento_form_validators.dart`: descricao não-vazia ≤200; valor via parse centavos não-nulo; categoria/forma default na carga) → repo.create/update → `context.pop(true)`; erro → snackbar. Cancelar → pop(false). `isSaving` bloqueia botão. Inicialização com valores do `lancamento` (formato BRL no campo valor, data/vencimento formatados).
  3. Em `app_router.dart`: novas rotas `/lancamentos` (lista, protegida por auth), `/lancamentos/novo`, `/lancamentos/:id` (só `authenticated`; reader carrega por `findById`); mover home: `/home` passa a montar a lista (ou home_screen vira a lista). Manter redirect da Fase 1 intacto (splash→/home se logado). `main.dart` sem mudança estrutural; ajustar somente se `home_screen` mudou de papel.
  </action>
  <verify>cd app && flutter analyze && flutter test</verify>
  <acceptance_criteria>
    - "lancamentos_list_screen.dart" contém "lancamentosStreamProvider" e "AlertDialog" (excluir) e FAB
    - "lancamento_form_screen.dart" contém "parseValorBRLParaCentavos" e "DropdownButtonFormField"
    - "app_router.dart" contém "/lancamentos/novo" e "/lancamentos/:id"
    - "flutter analyze" 0 issues; "flutter test" green (fakes)
  </acceptance_criteria>
  <done>Telas e rotas integradas; testes existentes ainda verdes</done>
</task>

<task type="auto">
  <name>Task 2: Testes widget do fluxo (lista, criar, editar, excluir) com fake repo</name>
  <files>app/test/features/lancamentos/presentation/lancamentos_flow_test.dart, app/test/support/fake_lancamentos_repository.dart, app/test/widget_smoke_test.dart (se necessário ajuste)</files>
  <read_first>app/test/support/fake_auth.dart, app/test/auth_flow_test.dart</read_first>
  <action>
  1. `app/test/support/fake_lancamentos_repository.dart`: in-memory com `StreamController<List<Lancamento>>.broadcast()` + `emit()`; guarda lista; imPrime `create/update/delete` no controller (espelho de `_seed`). Propriedade `deleted`/`updated` observáveis para asserts.
  2. `lancamentos_flow_test.dart` (pump num `ProviderScope` com overrides `lancamentosRepositoryProvider` e providers de auth fake reutilizados de `support/fake_auth.dart`; usar a árvore real com `MeuBolsoApp` e sobrescritas para não tocar rede):
     - T1: lista vazia → empty state visível.
     - T2: lista com 2 lançamentos → tiles renderizados com descricao e valor BRL ("R$ 12,34"; etc).
     - T3: preencher form novo (descricao, valor "12,34", categoria "Transporte") → salvar → fake vira 1 + lista mostra novo item (pop automático de volta na lista).
     - T4: erro de validação (descricao vazia / valor "abc" / valor "0") → mensagens inline e NENHUM create no fake.
     - T5: editar item existente → abrir, alterar descricao, salvar → fake.update chamado com valor correto.
     - T6: excluir → confirmar dialog → fake.delete; cancelar → nada acontece.
  </action>
  <verify>cd app && flutter test test/features/lancamentos/presentation/lancamentos_flow_test.dart && flutter analyze</verify>
  <acceptance_criteria>
    - "lancamentos_flow_test.dart" existe com os 6 cenários (T1..T6)
    - fake repo não toca rede; override via `lancamentosRepositoryProvider`
    - "flutter test" para a suíde inteira green (auth + smoke + flow)
  </acceptance_criteria>
  <done>Cobertura widget 6 cenários green — CRUD provado na árvore real</done>
</task>

<task type="auto">
  <name>Task 3: Build web + deploy GH Pages + verificação manual (checkpoint)</name>
  <files>app/build/web/, .github/workflows/deploy.yml, 02-VALIDATION.md</files>
  <read_first>app/lib/main.dart, .github/workflows/deploy.yml, .planning/phases/02-lan-amentos-manuais/02-RESEARCH.md</read_first>
  <action>
  1. `cd app && flutter build web --release --base-href=/projeto-financeiro/` com `--dart-define=SUPABASE_URL` e `--dart-define=SUPABASE_ANON_KEY` (valores do `.env`; rodar localmente com script que lê o .env — sem colocar segredo no comando do shell/log; usar PowerShell: `$env:` não; ler via file get-content do .env e passar). Confirmar `build/web/index.html` gerado.
  2. Push para GitHub (se não commitado ainda, verificar que plan 02-01 e 02-02 ingressaram). O deploy do Fase 1 já é automático (workflow deploy-pages dispara no push de `main`). Verificar run nova termina "success" e que `https://leonardorsvieira.github.io/projeto-financeiro/` responde 200.
  3. Checkpoint MANUAL (parar e pedir ao usuário):
     - Logar na URL pública; criar 1 lançamento (ex.: "Feira – mercado", R$ 89,90, Mercado, Pix, hoje).
     - Abrir 2ª aba anônima/outro navegador, logar na mesma conta → item aparece ≤ ~5s (sync realtime).
     - Editar o item numa aba → reflete na outra.
     - Excluir com confirmação → some nas duas.
     - Logout → home redireciona para login; sem acesso a dados.
     Pedir confirmação ao usuário antes de marcar complete.
  4. Atualizar `02-VALIDATION.md` (marcar tasks 02-03-01..04 e checkpoint como pass/fail).
  </action>
  <verify>cd app && (nete) gh run list --workflow=deploy.yml --limit 1</verify>
  <acceptance_criteria>
    - Build web generate com base-href; run do workflow mais recente "success"
    - URL pública retorna 200
    - Checkpoint manual: usuário confirmou 6 passos (criar, sync 2ª aba, editar sync, excluir sync, logout sem acesso, reload mantém)
    - 02-VALIDATION.md atualizado com resultados
  </acceptance_criteria>
  <done>Pronto em produção (web) + verificação humana registrada</done>
</task>

</tasks>

<verification>
- [ ] `cd app && flutter analyze` — 0 issues
- [ ] `cd app && flutter test` — green completo
- [ ] Build web release com base-href OK
- [ ] GH Pages run success, URL 200
- [ ] Checkpoint humano com todos os passos confirmados
- [ ] 02-VALIDATION.md coerente com execução
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- CRUD de lançamentos funcionando na web pública com sync realtime e isolamento por usuário
</success_criteria>

<output>
After completion, create `.planning/phases/02-lan-amentos-manuais/02-03-SUMMARY.md`
</output>