---
phase: 02-lan-amentos-manuais
plan: 02
type: execute
wave: 1
depends_on: [02-01]
files_modified: []
autonomous: true
requirements: [DSP-01]
must_haves:
  truths:
    - "Camada de dados Dart criada: model Lancamento, repository (stream+CRUD) e providers Riverpod"
    - "Representação monetária em centavos (bigint); conversão BRL via intl com symbol R$"
    - "flutter analyze e flutter test verdes"
  artifacts:
    - path: "app/lib/features/lancamentos/domain/lancamento.dart"
      provides: "Model imutável Lancamento com toMap/fromMap"
      min_lines: 40
    - path: "app/lib/features/lancamentos/data/supabase_lancamentos_repository.dart"
      provides: "Repository Supabase: stream(primaryKey, order), create/update/delete"
      contains: "stream"
    - path: "app/lib/features/lancamentos/application/lancamentos_providers.dart"
      provides: "StreamProvider<List<Lancamento>> + repository provider"
      contains: "StreamProvider"
---

<objective>
Construir a camada de dados Dart dos lançamentos (model, repository com stream realtime, providers Riverpod) e testes unitários — sem tocar na UI.

Purpose: Disponibilizar a lista sincronizada e as operações CRUD de forma testável; a UI (plan 02-03) consome esses providers.
Output: Feature `lancamentos` (domain + data + application) com analyze/test verdes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-lan-amentos-manuais/02-CONTEXT.md
@.planning/phases/02-lan-amentos-manuais/02-RESEARCH.md
@.planning/phases/02-lan-amentos-manuais/02-SKELETON.md
@.planning/phases/02-lan-amentos-manuais/02-VALIDATION.md
</context>

<threat_model>
Threats addressed by this plan:
- T-04-01: IDOR/scope — repository NUNCA envia user_id; RLS usa auth.uid() do JWT (custo zero no client).
- T-04-02: Payload malicioso/valores inválidos → parser de valor estrito (2 casas decimais), check negativo/zero rejeitado ANTES de chegar ao banco.
- T-04-04: Erro de arredondamento monetário → `valor_cents` int; conversão texto↔cents com precisão fixa; formatação só na UI.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Model Lancamento + conversor BRL (centavos) + testes unitários</name>
  <files>app/lib/features/lancamentos/domain/lancamento.dart, app/lib/features/lancamentos/domain/lancamento_converter.dart, app/test/features/lancamentos/domain/lancamento_test.dart</files>
  <read_first>app/pubspec.yaml, app/lib/features/auth/domain/auth_state.dart</read_first>
  <action>
  1. Em `app/pubspec.yaml`: adicionar `intl` à seção dependencies e rodar `flutter pub get`.
  2. `app/lib/features/lancamentos/domain/lancamento.dart`: clase `Lancamento` imutável (const ctor):

  Campos: `String id`, `String descricao`, `int valorCents`, `String categoria`, `String formaPagamento`, `DateTime data` (date-only), `DateTime? vencimento`, `String? obs`, `DateTime create createdAt/updatedAt`. Métodos: `Map<String, dynamic> toMap()` (ISO dates; valorCents int), factory `Lancamento.fromMap(Map)` (parse seguro de `data`/`vencimento` como date). Não conter lógica monetária além do int.

  3. `app/lib/features/lancamentos/domain/lancamento_converter.dart`:
     - `int? parseValorBRLParaCentavos(String texto)` → aceita "1,23" / "1.234,56" / "1234.56"; REJEITA 3+ casas decimais, negativo (usa ',', '.' como separador; pontos como milhar tolerados apenas se 3 dígitos). Retorna null em inválido.
     - `String formatoBRL(int centavos)` → `NumberFormat.currency(locale: 'pt_BR', symbol: r'R$')` (garantir `R$ 1.234,56`).
     - `String formatoData(DateTime)` → `dd/MM/yyyy` via `intl` (Locale('pt_BR')).
  4. `app/test/features/lancamentos/domain/lancamento_test.dart`: cubra:
     - parse: "12,34"→1234, "1.234,56"→123456, "999.99"→99999, "12.345"→INVÁLIDO(ou 3 casas), ""→null, "abc"→null, "0,001"→null.
     - formatoBRL: 123456→"R$ 1.234,56"; 99→"R$ 0,99"; 0→"R$ 0,00".
     - roundtrip fromMap/toMap (sem perda), data roundtrip.
     - formatoData: 2026-09-06→"06/09/2026".

  Não commitar valor com textos ambíguos (resumir nos testes quais casos ficaram).
  </action>
  <verify>cd app && flutter test test/features/lancamentos/domain/lancamento_test.dart && flutter analyze</verify>
  <acceptance_criteria>
    - "lancamento.dart" contém `toMap` e `fromMap`; valor é `int`
    - "lancamento_converter.dart" contém `parseValorBRLParaCentavos` e `formatoBRL` (com R$)
    - "lancamento_test.dart" cobre parse corrente/inválido, formato, roundtrip, formatoData
    - "flutter analyze" 0 issues; teste verde
  </acceptance_criteria>
  <done>Model + conversor com testes — valores sempre em centavos</done>
</task>

<task type="auto">
  <name>Task 2: Repository (stream + CRUD) e providers + testes</name>
  <files>app/lib/features/lancamentos/domain/lancamentos_repository.dart, app/lib/features/lancamentos/data/supabase_lancamentos_repository.dart, app/lib/features/lancamentos/application/lancamentos_providers.dart, app/test/features/lancamentos/application/lancamentos_providers_test.dart</files>
  <read_first>app/lib/core/env.dart, app/lib/features/auth/application/auth_controller.dart</read_first>
  <action>
  1. `app/lib/features/lancamentos/domain/lancamentos_repository.dart`: abstract com
     - `Stream<List<Lancamento>> watch({bool todos})` (lista por data desc),
     - `Future<Lancamento> create({descricao, valorCents, categoria, formaPagamento, data, vencimento, obs})`,
     - `Future<Lancamento> update(Lancamento l, {...campos})`,
     - `Future<void> delete(String id)`,
     - `Future<Lancamento?> findById(String id)`.
  2. `app/lib/features/lancamentos/data/supabase_lancamentos_repository.dart`: implementa com `Supabase.instance.client`. `watch`: `.from('lancamentos').stream(primaryKey: ['id']).order('data', ascending: false).map(...)` — SEM enviar user_id (RLS cuida). create/update/delete via `.insert`/`.update`/`.delete().eq('id',...)`. `update` do map com todos os campos serializados. Tratar `PostgrestException` rethrow com mensagem amigável (reuso de padrão de auth_errors, sem expor detalhes).
  3. `app/lib/features/lancamentos/application/lancamentos_providers.dart`:
     - `lancamentosRepositoryProvider` (ProviderRef) → SupabaseLancamentosRepository(), sobreescrito em testes.
     - `lancamentosStreamProvider = StreamProvider<List<Lancamento>>((ref) => ref.watch(lancamentosRepositoryProvider).watch())`.
     - `lancamentoByIdProvider = StreamProvider<Lancamento?>((ref) => ...)` leitura por id (ou shallow via watch map). Sem estado mutável além do stream (operações são fire-and-forget na UI).
  4. Teste de application (`lancamentos_providers_test.dart`): usar `ProviderContainer` + override do repository com fake in-memory (StreamController broadcast que emite lista e simula CRUD). Verificar: stream provider emite todos os itens; após `create` via repository fake, o watch reflete (manual add no controller). SEM rede.
  </action>
  <verify>cd app && flutter test test/features/lancamentos/application/lancamentos_providers_test.dart && flutter analyze</verify>
  <acceptance_criteria>
    - Repository abstrai Supabase; app só depende da interface
    - watch usa stream(primaryKey:['id']).order('data', descending) e NÃO referencia user_id
    - providers estão no padrão Riverpod 3 do projeto (iguais a auth_controller)
    - "flutter analyze" 0 issues; testes verdes
  </acceptance_criteria>
  <done>Repository + providers com teste fake — feature completa fora da UI</done>
</task>

</tasks>

<verification>
- [ ] `cd app && flutter analyze` — 0 issues
- [ ] `cd app && flutter test` — green (lancamento_test + lancamentos_providers_test)
- [ ] intl no pubspec
- [ ] Nenhum `user_id` no código client
- [ ] 02-SKELETON.md registra camada de dados
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Model monetário em centavos, repository stream+CRUD, providers prontos p/ UI
</success_criteria>

<output>
After completion, create `.planning/phases/02-lan-amentos-manuais/02-02-SUMMARY.md`
</output>