# Plan 03-03 — Fluxo fim-a-fim + Deploy (GEMINI_API_KEY) + Checkpoint

**Phase:** 3 | **Plan:** 3 of 3 | **Status:** Ready | **Date:** 2026-09-06
**Wave:** 2

## Objetivo
Garantir o ciclo completo "fala → confirmação → salvar → lista (realtime)" com testes, publicar com a chave Gemini e fechar a fase com checkpoint manual.

## Tasks

### 1. Teste fim-a-fim
- `test/features/ditado/presentation/ditado_flow_test.dart` com `wrapWithFakes` + `MeuBolsoApp`:
  - V1: FAB → Ditar → (fake gravador) → rascunho pré-preenchido em S8 → Salvar → item na lista.
  - V2: correção por voz (fake grava "chipe" → descricao vira "Chipe") → Salvar persistido.
  - V3: valor ausente → Salvar bloqueado (validador) até preencher.
- Fakes: `fake_ditado_repository`, `fake_audio_recorder_service` consumidos via providers.

### 2. Deploy com chave
- Adicionar secret `GEMINI_API_KEY` no GitHub (usuário fornece key do AI Studio).
- Workflow `deploy.yml`: `--dart-define=GEMINI_API_KEY=${{ secrets.GEMINI_API_KEY }}`.
- `.env` local (gitignored) atualizado; `supabase`-like: dart-define no build local.
- Push → `gh run list` → GET URL = 200.

### 3. Checkpoint manual (UAT V10..V14)
- Pedir ao usuário validar no real:
  - V10 ditar "almoço quarenta reais" → confirmação correta;
  - V11 categoria/forma quando citadas; ausente → vazio/foco;
  - V12 correção por voz campo-a-campo e por toque;
  - V13 Salvar → aparece na lista em 2 abas (realtime);
  - V14 erro/mic negado não quebra fluxo manual.

### 4. Fechamento
- Atualizar `03-VALIDATION.md` (marcar ✅/⏳), escrever `03-01/02/03-SUMMARY.md`, `state.advance-plan` x3, `state.complete-phase --phase 3`, STATE/ROADMAP, commit final.

## Verification
- 32+ testes antigos verdes + novos (V1..V3).
- Workflow GH Pages success → URL 200.
- Checkpoint manual ✅ (usuário).

## Dependencies
- Plan 03-02 (telas); usuário fornece `GEMINI_API_KEY`.