---
phase: 02-lan-amentos-manuais
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [PLAT-04]
must_haves:
  truths:
    - "Tabela lancamentos criada no Supabase remoto com RLS, índice e realtime habilitado"
    - "Policies restringem acesso ao dono (auth.uid() = user_id); anon não vê nada"
    - "SQL versionado em supabase/migrations e aplicável via CLI/API"
  artifacts:
    - path: "supabase/migrations/*_create_lancamentos.sql"
      provides: "Migration idempotente: tabela, índices, RLS, policies e publicação realtime"
      contains: "enable row level security"
    - path: "02-SKELETON.md"
      provides: "Contrato arquitetural atualizado: schema lancamentos + camada de dados"
      contains: "Architectural Decisions"
---

<objective>
Criar e aplicar no Supabase remoto o schema `lancamentos` com isolamento por dono (RLS) e Realtime habilitado, versionado em migration — provando PLAT-04 (dados sincronizados e isolados por usuário).

Purpose: O fundamento de dados da Fase 2. Sem isso, nenhuma tela persiste. Aplicação no remoto com verificação real (tabela presente, RLS ativa, anon bloqueado).
Output: Migration `create_lancamentos` no git + estado real do banco remoto verificado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-lan-amentos-manuais/02-CONTEXT.md
@.planning/phases/02-lan-amentos-manuais/02-RESEARCH.md
@.planning/phases/02-lan-amentos-manuais/02-VALIDATION.md
</context>

<threat_model>
Threats addressed by this plan (ASVS L1):
- T-04-01: IDOR/scope — usuário lê/altera lançamentos de outro → RLS com auth.uid() = user_id em todas as policies; app nunca envia user_id (default auth.uid()).
- T-04-03: Tabela exposta a anon (dados financeiros sensíveis) → RLS enable, policies apenas `to authenticated`; verificação real de que anon não lê nada.
- T-04-02: Payloads malformados → check constraints (descricao 1..200, valor_cents > 0, obs <= 500) no banco além da validação client.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Escrever migration create_lancamentos e atualizar 02-SKELETON.md</name>
  <files>supabase/migrations/20260906120000_create_lancamentos.sql, 02-SKELETON.md</files>
  <read_first>.planning/phases/02-lan-amentos-manuais/02-CONTEXT.md, .planning/phases/02-lan-amentos-manuais/02-RESEARCH.md, 01-SKELETON.md</read_first>
  <action>
  Escrever `supabase/migrations/20260906120000_create_lancamentos.sql` com a migration IDEMPOTENTE:

  ```sql
  create table if not exists public.lancamentos (
    id uuid primary key default gen_random_uuid(),
    user_id uuid not null default auth.uid() references auth.users(id) on delete cascade,
    descricao text not null check (char_length(descricao) between 1 and 200),
    valor_cents bigint not null check (valor_cents > 0),
    categoria text not null,
    forma_pagamento text not null,
    data date not null default current_date,
    vencimento date,
    obs text check (obs is null or char_length(obs) <= 500),
    created_at timestamptz not null default now(),
    updated_at timestamptz not null default now()
  );

  create index if not exists lancamentos_user_data_idx on public.lancamentos (user_id, data desc);
  create index if not exists lancamentos_user_created_idx on public.lancamentos (user_id, created_at desc);

  alter table public.lancamentos enable row level security;

  drop policy if exists lancamentos_select_own on public.lancamentos;
  create policy lancamentos_select_own on public.lancamentos for select to authenticated using (auth.uid() = user_id);
  drop policy if exists lancamentos_insert_own on public.lancamentos;
  create policy lancamentos_insert_own on public.lancamentos for insert to authenticated with check (auth.uid() = user_id);
  drop policy if exists lancamentos_update_own on public.lancamentos;
  create policy lancamentos_update_own on public.lancamentos for update to authenticated using (auth.uid() = user_id) with check (auth.uid() = user_id);
  drop policy if exists lancamentos_delete_own on public.lancamentos;
  create policy lancamentos_delete_own on public.lancamentos for delete to authenticated using (auth.uid() = user_id);

  -- Realtime (stream do supabase_flutter emitindo mudanças):
  alter publication supabase_realtime add table public.lancamentos;
  ```

  Nota: `alter publication ... add table` é idempotente por natureza (Postgres ignora duplicidade sem erro? — em alguns casos emite notice; ok). Se a publicação `supabase_realtime` não existir no projeto remoto, criá-la antes: `create publication if not exists supabase_realtime;` — nesse caso criar começo da migration com `do $$ begin` para ser defensivo (documentar no summary o que ocorreu no remoto).

  Criar/atualizar `02-SKELETON.md`: copiar o 01-SKELETON.md, mudar `**Phase:** 2`, marcar fase 2 na "Subsequent Slice Plan" como em progresso, adicionar decisões novas (D-14 a D-21), e registrar no "Stack Touched": Database (schema + RLS + realtime) [ok].

  Rodar `flutter analyze`/`flutter test` NÃO é necessário neste plan (sem código Dart).
  </action>
  <verify>Test-Path supabase/migrations/20260906120000_create_lancamentos.sql && (Select-String -Path supabase/migrations/20260906120000_create_lancamentos.sql -Pattern "enable row level security").Count -eq 1</verify>
  <acceptance_criteria>
    - "supabase/migrations/20260906120000_create_lancamentos.sql" existe e contém "enable row level security" e "alter publication supabase_realtime add table public.lancamentos"
    - Arquivo não contém credenciais (grep por "sbp_"/"anon"/"service_role"): NENHUM match
    - "02-SKELETON.md" contém "## Architectural Decisions" e reflete schema de lancamentos + RLS e realtime
  </acceptance_criteria>
  <done>Migration escrita e versionada; SKELETON atualizado</done>
</task>

<task type="auto">
  <name>Task 2: Aplicar migration no Supabase remoto e verificar</name>
  <files>supabase/migrations/</files>
  <read_first>.planning/phases/02-lan-amentos-manuais/02-RESEARCH.md, supabase/config.toml</read_first>
  <action>
  Aplicar a migration no projeto remoto `tkfhthotspehsgvmpsjm` (link já feito, sa-east-1):

  1. Preferir: `supabase db push` (rodar relativo a `supabase/`; CLI em `C:\Users\leona\AppData\Local\Programs\Supabase\supabase.exe`). Se o CLI pedir token de acesso: o token do usuário pode ter sido revogado — tentar usar `$env:SUPABASE_ACCESS_TOKEN` (pedir novo ao usuário se indisponível).
  2. Fallback: Management API `POST https://api.supabase.com/v1/projects/tkfhthotspehsgvmpsjm/database/query` com body `{"query": "<conteúdo do arquivo>"}` e header `Authorization: Bearer <pat>`. Usar o mesmo PAT se ainda válido; se revogado, pedir novo ao usuário (é a única porta para schema sem CLI/docker).
  3. Verificar com uma query no banco remoto (via `supabase db query`/Management API):
     - `select table_name, row_security from information_schema.tables where table_schema='public' and table_name='lancamentos';` → presencia `lancamentos` | `YES`
     - `select polname from pg_policies where tablename='lancamentos';` → 4 policies (select/insert/update/delete)
     - `select count(*) from pg_publication_tables where pubname='supabase_realtime' and tablename='lancamentos';` → 1
  4. Teste de bloqueio anon: `select count(*) from public.lancamentos;` com role anon (sem Auth header) → deve retornar erro de permissão OU 0 (nunca linhas). Registrar o resultado literal no summary.
  5. Se `db push` aplicar, confirmar que NADA além da migration foi alterado no remoto (diff limpo).

  Nunca rodar `supabase db reset`. Nunca logar senha do banco (postgres não é necessário).
  </action>
  <verify>$supabase = "C:\Users\leona\AppData\Local\Programs\Supabase\supabase.exe"; & $supabase db push --linked</verify>
  <acceptance_criteria>
    - Query de verificação no remoto confirma: tabela existe, row_security=YES, 4 policies, realtime publicado
    - Consulta anon retorna 0 linhas ou erro de permissão (nunca dados)
    - Nenhuma credencial apareceu em logs/saída
  </acceptance_criteria>
  <done>Migration aplicada e verificada no remoto (RLS ativa, realtime on, anon bloqueado)</done>
</task>

</tasks>

<verification>
- [ ] Migration existe no git com SQL idempotente
- [ ] Remoto: tabela lancamentos + row_security YES + 4 policies + realtime publication ok
- [ ] Anon bloqueado (0 linhas / permissão negada)
- [ ] 02-SKELETON.md atualizado
- [ ] Sem credenciais em arquivos commitados
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Schema de lançamentos no ar, RLS por dono, realtime publicado, anon sem acesso
</success_criteria>

<output>
After completion, create `.planning/phases/02-lan-amentos-manuais/02-01-SUMMARY.md`
</output>