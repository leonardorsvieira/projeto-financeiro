---
id: "04-02"
title: "Recorrência mensal com geração automática (lazy)"
status: "Planned"
phase: "04-vencimentos-itens-e-recorrencias"
files_modified:
  - "app/lib/features/lancamentos/data/lancamentos_repository.dart"
  - "app/lib/features/lancamentos/presentation/lancamentos_list_screen.dart"
  - "app/lib/features/lancamentos/presentation/lancamento_form_screen.dart"
  - "app/lib/features/lancamentos/application/lancamentos_providers.dart"
tasks:
  - id: "04-02-01"
    description: "Implementar `LancamentosRepository.gerarProximaCopiaSeFixa(lancamento)`: se `fixoMensal && serieId != null`, calcular próximo mês (mesmo dia, ajuste fim de mês), criar cópia com `data` = próximo mês, `serieId` herdado, `id` novo, `createdAt/updatedAt` = now; inserir via Supabase"
    type: "code"
    depends_on: ["04-01-05"]
    acceptance_criteria:
      - "Método cria 1 cópia por chamada"
      - "Cálculo de próximo mês correto (ex.: 15/09 → 15/10; 31/01 → 28/02 ou 29/02)"
      - "Cópia tem `fixoMensal = true` e mesmo `serieId`"
      - "Não duplica se já existe cópia para aquele mês (idempotência por `user_id + serie_id + data` = mesmo mês)"
  - id: "04-02-02"
    description: "Implementar `LancamentosRepository.ensureVigenteCopies()`: ao abrir lista (stream), para cada série fixa do usuário, garantir que existe cópia para o mês atual e próximo; chamar `gerarProximaCopiaSeFixa` se faltar"
    type: "code"
    depends_on: ["04-02-01"]
    acceptance_criteria:
      - "Chamado no provider de stream da lista (ex.: no `StreamProvider` ou no `FutureProvider` inicial)"
      - "Não gera cópias passadas (apenas mês atual + próximo)"
      - "Lazy: só roda quando lista é aberta/atualizada"
  - id: "04-02-03"
    description: "Adicionar ação 'Excluir série' no menu do lançamento (S5): ao excluir lançamento com `fixoMensal && serieId != null`, mostrar dialog 'Excluir apenas este' / 'Excluir série toda'; se 'série toda', deletar todos com mesmo `serieId`"
    type: "code"
    depends_on: ["04-01-04"]
    acceptance_criteria:
      - "Menu do item (⋮) mostra 'Excluir' e 'Excluir série' quando fixa"
      - "'Excluir série' deleta todos os lançamentos com mesmo `serieId` (1 query)"
      - "Confirmação clara antes de deletar série"
      - "Stream atualiza em tempo real (RLS + realtime)"
  - id: "04-02-04"
    description: "Atualizar formulário S6: quando checkbox 'Despesa fixa mensal' marcado, pré-preencher dia do vencimento com o dia da data atual (ou vencimento se já setado); desabilitar edição de data variável (opcional — manter data como referência)"
    type: "code"
    depends_on: ["04-01-03"]
    acceptance_criteria:
      - "Checkbox fixa → mostra campo 'Dia do vencimento (1-31)'"
      - "Valor padrão = dia da data do lançamento"
      - "Ao salvar fixa, `vencimento` = próximo mês no dia informado"
  - id: "04-02-05"
    description: "Testes: repositório (gerar cópia, idempotência, ensureVigenteCopies, excluir série); UI (menu excluir série, checkbox fixa com dia); integração (fluxo completo fixa → cópia aparece)"
    type: "test"
    depends_on: ["04-02-01", "04-02-02", "04-02-03", "04-02-04"]
    acceptance_criteria:
      - "Testes unitários passam"
      - "Teste de integração: salvar fixa → abrir lista → cópia do próximo mês visível"
  - id: "04-02-06"
    description: "`flutter analyze` + suite completa de testes"
    type: "test"
    depends_on: ["04-02-05"]
    acceptance_criteria:
      - "`flutter analyze` limpo"
      - "`flutter test` 100% passing"
verification:
  - "Criar despesa fixa mensal (ex.: aluguel dia 5) → salvar → lista mostra ícone fixa + badge vencimento"
  - "Recarregar app / pull-to-refresh → cópia do mês seguinte aparece automaticamente (sem recarregar manualmente)"
  - "Excluir série → todos os meses daquela série somem da lista"
  - "Editar cópia de um mês → não afeta outros meses (cada cópia independente)"
  - "Meses sem o dia (31/fev) → cópia cai no último dia do mês (28/29)"