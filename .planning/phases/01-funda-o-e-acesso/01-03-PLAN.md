---
phase: 01-funda-o-e-acesso
plan: 03
type: execute
wave: 2
depends_on: ["01-01", "01-02"]
files_modified: [app/lib/main.dart, app/lib/features/auth/, app/lib/features/home/, app/lib/router/, .github/workflows/deploy.yml, app/web/index.html, app/web/404.html]
autonomous: false
requirements: [PLAT-01, PLAT-03]
must_haves:
  truths:
    - "Usuário cria conta com e-mail/senha e faz login no app"
    - "Sessão persiste ao fechar e reabrir o app (fica logado)"
    - "App é acessível pela Web em URL pública (GitHub Pages)"
    - "Sem sessão, o app não mostra a home (redireciona para /login)"
  artifacts:
    - path: "app/lib/features/auth/presentation/login_screen.dart"
      provides: "Tela de login (UI-SPEC S2)"
      min_lines: 80
    - path: "app/lib/features/auth/presentation/signup_screen.dart"
      provides: "Tela de cadastro (UI-SPEC S3)"
      min_lines: 80
    - path: "app/lib/router/app_router.dart"
      provides: "Rotas go_router com redirect por sessão"
      min_lines: 40
    - path: ".github/workflows/deploy.yml"
      provides: "Deploy web no GitHub Pages"
      contains: "deploy-pages"
  key_links:
    - from: "app/lib/features/auth/presentation/login_screen.dart"
      to: "supabase.auth.signInWithPassword"
      via: "chamada no submit"
      pattern: "signInWithPassword"
    - from: "app/lib/router/app_router.dart"
      to: "supabase.auth.onAuthStateChange"
      via: "redirect condicional por sessão"
      pattern: "onAuthStateChange"
---

<objective>
Implementar o fluxo de autenticação completo (login, cadastro, sessão persistente, logout) conforme UI-SPEC S1-S4, e publicar a versão Web no GitHub Pages.

Purpose: Entrega o core value de acesso desta fase — usuário cria conta e entra, permanecendo logado entre aberturas, em todos os alvos (PLAT-01) e acessível de qualquer lugar via URL pública (PLAT-03).
Output: Telas de splash/login/cadastro/home conectadas ao Supabase, sessão persistente, build web publicado em https://leonardorsvieira.github.io/projeto-financeiro/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-funda-o-e-acesso/01-CONTEXT.md
@.planning/phases/01-funda-o-e-acesso/01-RESEARCH.md
@.planning/phases/01-funda-o-e-acesso/01-UI-SPEC.md
@.planning/phases/01-funda-o-e-acesso/01-SKELETON.md
@.planning/phases/01-funda-o-e-acesso/01-01-SUMMARY.md
@.planning/phases/01-funda-o-e-acesso/01-02-SUMMARY.md
</context>

<threat_model>
Threats addressed by this plan (ASVS L1):
- T-03-01: NAVEGAÇÃO protegida — rota home sem sessão → redirect p/ /login (auth state via go_router, sem segredo no código).
- T-03-02: Credenciais/segredos no web build → apenas URL+anon key (públicas por projeto) via --dart-define; NENHUM service_role; .env fora do build.
- T-03-03: Erros de auth vazando detalhes → mensagens de erro amigáveis (e-mail não encontrado / credenciais inválidas), sem stack traces no UI.
- T-03-04: Sessão armazenada insegura → default shared_preferences OK na Fase 1 (sem dados sensíveis); hardening flutter_secure_storage registrado no SKELETON p/ fase 2.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Feature de auth + rotas com redirect (go_router)</name>
  <files>app/lib/core/env.dart, app/lib/features/auth/data/auth_repository.dart, app/lib/features/auth/application/auth_controller.dart, app/lib/features/auth/domain/auth_state.dart, app/lib/router/app_router.dart, app/lib/main.dart</files>
  <read_first>app/lib/main.dart, app/lib/features/home/domain/app_routes.dart, app/pubspec.yaml, .planning/phases/01-funda-o-e-acesso/01-SKELETON.md</read_first>
  <action>
  Dentro de `app/lib/`, criar a estrutura feature-first definida no SKELETON:

  - `core/env.dart`: `class AppEnv { static const supabaseUrl = String.fromEnvironment('SUPABASE_URL'); static const supabaseAnonKey = String.fromEnvironment('SUPABASE_ANON_KEY'); }` (constantes ofuscadas por definição; valores vêm do build).
  - `features/auth/domain/auth_state.dart`: enum `AuthStatus { unauthenticated, authenticating, authenticated }` + classe `AuthState` (status + user email).
  - `features/auth/data/auth_repository.dart`: `class AuthRepository` que embrulha `Supabase.instance.client.auth` — métodos `signUp(email, senha)`, `signIn(email, senha)`, `signOut()`, e expõe `Stream<AuthState>` derivado de `onAuthStateChange` (mapear `session.user?.email`).
  - `features/auth/application/auth_controller.dart`: `NotifierProvider<AuthController, AsyncValue<AuthState>>` com Riverpod; `build` escuta o stream do repositório; métodos `signIn/signUp/signOut` que lançam/mapeiam erros para o UI.
  - `main.dart`: registrar `ProviderScope(overrides: [authControllerProvider.overrideWith...])` OU prover o `AuthRepository` via `Provider`. Inicialização real: `Supabase.initialize(url: AppEnv.supabaseUrl, anonKey: AppEnv.supabaseAnonKey, authOptions: FlutterAuthClientOptions(persistSession: true))` (persistência padrão). IMPORTANTE: rodar o app com `--dart-define=SUPABASE_URL=... --dart-define=SUPABASE_ANON_KEY=...` lendo os valores de `.env` na raiz (adicionar `run_app.ps1` ou documentar comando no resumo).
  - `router/app_router.dart`: `GoRouter` com `/login`, `/signup`, `/home`; `redirect` baseado no `AuthState`: sessão nula OU `unauthenticated` + target `/home` → `/login`; `authenticated` + target `/login|/signup` → `/home`. Splash como tela inicial enquanto `authState` está resolvendo (watch async).
  </action>
  <verify>cd app && flutter analyze</verify>
  <acceptance_criteria>
    - "app/lib/features/auth/data/auth_repository.dart" contém "signInWithPassword", "signUp" e "onAuthStateChange"
    - "app/lib/router/app_router.dart" contém "redirect" e referências a "/login" e "/home"
    - "app/lib/core/env.dart" contém "String.fromEnvironment('SUPABASE_URL')" e "String.fromEnvironment('SUPABASE_ANON_KEY')"
    - "flutter analyze" sai com 0 issues
  </acceptance_criteria>
  <done>Auth feature + roteamento com redirect por sessão implementados e analisados OK</done>
</task>

<task type="auto">
  <name>Task 2: Telas de Splash, Login, Cadastro e Home per UI-SPEC</name>
  <files>app/lib/features/auth/presentation/splash_screen.dart, app/lib/features/auth/presentation/login_screen.dart, app/lib/features/auth/presentation/signup_screen.dart, app/lib/features/home/presentation/home_screen.dart, app/lib/theme/app_theme.dart</files>
  <read_first>app/lib/features/auth/data/auth_repository.dart, app/lib/features/auth/application/auth_controller.dart, app/lib/router/app_router.dart, .planning/phases/01-funda-o-e-acesso/01-UI-SPEC.md</read_first>
  <action>
  Implementar as 4 telas conforme UI-SPEC:

  - `splash_screen.dart`: logo "Meu Bolso" + spinner; sem botões (redirect automático via rota).
  - `login_screen.dart` (S2): `Form` com `TextFormField` de e-mail (keyboardType email, autofillHints[email]) e senha (`obscureText`, toggle de visibilidade com tooltip); botão `FilledButton` full-width "Entrar" com estado loading (desabilitar + spinner enquanto `authController` está autenticando); link "Não tem conta? Criar conta" navegando a `/signup`; erros exibidos inline com `textError` (nunca snackbar para erros de credencial). Usar `authController.signIn(email, senha)` capturando exceções (AuthException → mensagem amigável).
  - `signup_screen.dart` (S3): e-mail + senha (min 6) + confirmar senha; validação local (regex `^\S+@\S+\.\S+$`, senha ≥ 6, confirmar == senha); botão "Criar conta" → `authController.signUp`; link para `/login`; mensagens inline (e-mail em uso, senha fraca).
  - `home_screen.dart` (S4): AppBar "Meu Bolso" + botão "Sair" com `PopupMenuButton` confirmando `signOut()` via `AuthRepository`; corpo centralizado "Em breve — seus lançamentos aparecem aqui"; mostra e-mail do usuário logado; máx 480 px no web.
  - Ajustar `app_theme.dart` apenas se necessário p/ tokens (reutilizar do plan 01-01).
  </action>
  <verify>cd app && flutter analyze</verify>
  <acceptance_criteria>
    - "login_screen.dart" contém "signInWithPassword" (via repositório) e "obscureText"
    - "signup_screen.dart" contém validação "confirmar" e chamada "signUp"
    - "home_screen.dart" contém "signOut" e "Fase 2" / texto placeholder
    - "flutter analyze" 0 issues
  </acceptance_criteria>
  <done>Telas de splash/login/cadastro/home implementadas com estados de loading/erro</done>
</task>

<task type="auto">
  <name>Task 3: Widget tests de auth (validação + navegação protegida)</name>
  <files>app/test/auth_flow_test.dart, app/test/router_redirect_test.dart</files>
  <read_first>app/lib/router/app_router.dart, app/lib/features/auth/application/auth_controller.dart, app/lib/features/auth/data/auth_repository.dart</read_first>
  <action>
  Criar testes de widget (`flutter_test`) sem dependência da rede:

  - `test/router_redirect_test.dart`: com `AuthRepository` falso (sobrescrever o provider via `ProviderScope(overrides: [...])`), verificar que com sessão nula a rota inicial é `/login` (não `/home`), e que com `AuthState.authenticated` a rota inicial é `/home`.
  - `test/auth_flow_test.dart`: validar UI — campo e-mail fica com `errorText` para e-mail inválido; senha < 6 gera erro no cadastro; senhas diferentes geram erro; apertar "Entrar" com repositório falso que lança `AuthException` mostra mensagem de erro amigável (não stack trace).
  Rodar `flutter analyze && flutter test`.
  </action>
  <verify>cd app && flutter test</verify>
  <acceptance_criteria>
    - "flutter test" passa (todos os novos testes verdes junto com o smoke test)
    - "app/test/router_redirect_test.dart" existe e contém casos de sessão nula → "/login" (assert explícito)
    - "app/test/auth_flow_test.dart" existe
    - nenhum teste depende de credenciais reais (seed-fake)
  </acceptance_criteria>
  <done>Testes de auth verdes; navegação protegida comprovada por teste</done>
</task>

<task type="auto">
  <name>Task 4: Build web com base-href e deploy no GitHub Pages</name>
  <files>app/web/index.html, app/web/404.html, .github/workflows/deploy.yml</files>
  <read_first>app/pubspec.yaml, .planning/phases/01-funda-o-e-acesso/01-RESEARCH.md</read_first>
  <action>
  1. Configurar build web para caminho base: `<base href="/projeto-financeiro/">` em `app/web/index.html`.
  2. Criar `app/web/404.html` copiando o conteúdo do `index.html` gerado posteriormente (fallback para SPA em páginas recarregadas).
  3. Criar `.github/workflows/deploy.yml`: workflow `deploy-pages` que roda em push na `main` quando `app/**` mudar:
     - `permissions`: `contents: read`, `pages: write`, `id-token: write`
     - `environment`: `{ name: github-pages, url: \${{ steps.deployment.outputs.page_url }} }`
     - steps: `actions/checkout@v4` → `subosito/flutter-action@v2` (lê `app/pubspec.yaml` p/ versão) → `cd app && flutter pub get` → `flutter build web --release --dart-define=...` com `SUPABASE_URL`/`SUPABASE_ANON_KEY` lidas de **repository secrets** (criar via `gh secret set`) → garantir `.nojekyll` em `build/web` (`Set-Content`/touch) → `actions/configure-pages@v5` → `actions/upload-pages-artifact@v3` (path `app/build/web`) → `actions/deploy-pages@v4`.
     - IMPORTANTE: URL/anon são públicos, mas via secrets do repo − **nunca hardcoded no workflow**.
  4. Habilitar Pages via GitHub API: `gh api -X POST repos/leonardorsvieira/projeto-financeiro/pages -f build_type=workflow` (ou configurar via Settings → Pages → "GitHub Actions").
  5. Empurrar `main` e verificar: o workflow roda (watch com `gh run watch`) e a URL https://leonardorsvieira.github.io/projeto-financeiro/ responde 200.
  </action>
  <verify>cd app && flutter build web --release --base-href=/projeto-financeiro/; gh run watch; Invoke-WebRequest https://leonardorsvieira.github.io/projeto-financeiro/ -UseBasicParsing</verify>
  <acceptance_criteria>
    - "app/web/index.html" contém `<base href="/projeto-financeiro/">`
    - ".github/workflows/deploy.yml" contém "deploy-pages" e "upload-pages-artifact" e usa "secrets"
    - workflow de deploy passou (gh run list verde no ref de main)
    - GET https://leonardorsvieira.github.io/projeto-financeiro/ retorna 200
  </acceptance_criteria>
  <done>Web publicado e acessível via URL pública do GitHub Pages</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>App autenticado publicado — URL: https://leonardorsvieira.github.io/projeto-financeiro/ (e localmente via flutter run)</what-built>
  <how-to-verify>Abra a URL e verifique: (1) tela de login aparece (não a home); (2) crie conta com um e-mail e senha → vai para a home; (3) feche a aba e reabra a URL → continua logado (sessão persistida); (4) clique em "Sair" → volta ao login; (5) tente /home sem logar (recarregar com /home) → cai no login. Apenas inspeção visual/comportamental — sem comandos CLI.</how-to-verify>
  <resume-signal>Type "approved" ou descreva o que falhou</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd app && flutter analyze` — 0 issues
- [ ] `cd app && flutter test` — green
- [ ] `cd app && flutter build web --release --base-href=/projeto-financeiro/` — ok
- [ ] GitHub Pages publicando e respondendo 200
- [ ] Checkpoint humano aprovou: create conta → login → sessão persistida → logout → redirect
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- PLAT-01 (login/sessão) e PLAT-03 (acesso web) comprovados por teste + verificação humana
</success_criteria>

<output>
After completion, create `.planning/phases/01-funda-o-e-acesso/01-03-SUMMARY.md`
</output>