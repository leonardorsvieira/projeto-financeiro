---
id: "04-03"
title: "Ditado de vencimento e itens na extração da IA"
status: "Planned"
phase: "04-vencimentos-itens-e-recorrencias"
files_modified:
  - "app/lib/features/ditado/data/gemini_prompt.dart"
  - "app/lib/features/ditado/domain/rascunho_lancamento.dart"
  - "app/lib/features/ditado/presentation/confirmacao_ditado_screen.dart"
  - "app/lib/features/ditado/presentation/lancamento_ditado_screen.dart"
  - "app/lib/features/ditado/application/ditado_providers.dart"
tasks:
  - id: "04-03-01"
    description: "Estender `RascunhoLancamento`: adicionar campo `itens: List<RascunhoItem>?` (onde `RascunhoItem = {descricao, valor_reais}`); atualizar `fromJson`/`toJson`/`copyWith`"
    type: "code"
    depends_on: []
    acceptance_criteria:
      - "Modelo serializa/desserializa `itens` corretamente"
      - "Compatível com JSON atual (itens optional)"
  - id: "04-03-02"
    description: "Atualizar `GeminiPrompt._instrucaoReconhecer`: adicionar campo `itens` no JSON de resposta (`[{descricao, valor_reais}]`); adicionar regras: itens opcionais; se presentes, cada item tem `descricao` (string) e `valor_reais` (string com vírgula); exemplos de ditado natural"
    type: "code"
    depends_on: ["04-03-01"]
    acceptance_criteria:
      - "Prompt instrui IA a extrair itens quando ditados"
      - "JSON de resposta inclui `itens` array (pode ser null/vazio)"
      - "Regras de soma: `valor_cents` final = soma dos itens (se houver), senão `valor_reais` do campo raiz"
  - id: "04-03-03"
    description: "Atualizar `GeminiPrompt` para vencimento ditado como 'dia 15': adicionar regra 'se usuário ditar apenas o dia (ex.: "vence dia 15"), interprete como o próximo mês com esse dia; hoje é {hoje}'"
    type: "code"
    depends_on: ["04-03-02"]
    acceptance_criteria:
      - "Prompt processa 'dia 15' → data correta (próximo 15)"
      - "Mantém comportamento atual para datas completas"
  - id: "04-03-04"
    description: "Atualizar `ConfirmacaoDitadoScreen`: se `rascunho.itens != null && itens.isNotEmpty`, renderizar seção itens editável (ListView.builder com descrição + valor editáveis, add/remove linhas); exibir soma total dos itens; botão 'Salvar' valida: se há itens, `valor_cents` = soma (não editável direto); se não há itens, usa `valor_reais` do rascunho"
    type: "code"
    depends_on: ["04-03-01"]
    acceptance_criteria:
      - "Tela mostra itens do rascunho quando existirem"
      - "Itens editáveis (descrição + valor) antes de salvar"
      - "Soma exibida e usada como valor final do lançamento"
      - "Validação: soma > 0, descrição não vazia"
  - id: "04-03-05"
    description: "Atualizar fluxo de ditado (`LancamentoDitadoScreen` + providers): passar `itens` do rascunho para confirmação; no save, converter `RascunhoLancamento.itens` → `Lancamento.itens` (List<Item> com `valor_cents`); se `fixoMensal` detectado no ditado (ex.: 'fixo todo mês'), pré-marcar checkbox na confirmação (opcional v2, mas deixar hook)"
    type: "code"
    depends_on: ["04-03-02", "04-03-04"]
    acceptance_criteria:
      - "Ditado 'comprei arroz 20, feijão 12' → confirmação mostra 2 itens editáveis"
      - "Salvar cria lançamento com itens populados"
      - "Valor final = soma dos itens confirmados"
  - id: "04-03-06"
    description: "Atualizar `_instrucaoCorrecao` (correção campo a campo) para suportar correção de `itens` via voz: 'Corrija os itens' → IA devolve array completo atualizado"
    type: "code"
    depends_on: ["04-03-02"]
    acceptance_criteria:
      - "Correção por voz de itens funciona (redita lista completa)"
      - "Mantém compatibilidade com correção de outros campos"
  - id: "04-03-07"
    description: "Testes: prompt (JSON com itens, regra dia 15); rascunho (serialização itens); confirmação UI (edição itens, soma, validação); fluxo completo ditado→confirmação→salvar com itens"
    type: "test"
    depends_on: ["04-03-01", "04-03-02", "04-03-04", "04-03-05", "04-03-06"]
    acceptance_criteria:
      - "Testes unitários passam"
      - "Teste de integração: ditado com itens → confirmação editável → lançamento salvo com itens"
  - id: "04-03-08"
    description: "`flutter analyze` + suite completa de testes"
    type: "test"
    depends_on: ["04-03-07"]
    acceptance_criteria:
      - "`flutter analyze` limpo"
      - "`flutter test` 100% passing"
verification:
  - "Ditar: 'comprei no mercado arroz vinte reais, feijão doze reais, carne quarenta e cinco reais' → IA extrai 3 itens com valores"
  - "Confirmação mostra 3 linhas editáveis; soma = 77,00; salvar cria lançamento com itens"
  - "Ditar: 'conta de luz cento e cinqüenta que vence dia 15' → vencimento = próximo dia 15"
  - "Corrigir itens por voz na confirmação: 'Corrigir itens... adicionei tomate dez reais' → IA devolve lista atualizada"
  - "Sem itens ditados: comportamento atual preservado (valor único, sem seção itens)"