# Plan 08-04: Dividendos/rendimentos mensais

## Goal
Registrar dividendos/rendimentos por ativo e vê-los somados por mês (INV-06).

## Context
- `rendimentos_investimento` (tabela) criada na migration 08-01 (D-02/D-07): tipos dividendo/juros/rendimento/outro.
- Fase 7 mostrou o padrão de agregados do mês no dashboard (ResumoMes).

## Scope
**Inclui:**
- Domain `RendimentoInvestimento` (id, investimentoId, tipo, valorCents, data) + repository (interface/supabase/fake: `watchTodos`, `create`, `delete`) + providers (`rendimentosStreamProvider`, `rendimentosPorAtivoProvider.family`).
- UI na tela detalhe do ativo: seção "Rendimentos" (lista + FAB registrar).
- `RendimentoFormScreen` (novo/edição): Dropdown tipo (Dividendo/Juros/Rendimento/Outro), valor R$, data; salvar/editar/excluir.
- Agregação por mês: provider `rendimentosPorMesProvider` (soma `valor_cents` por `year-month`, ordenado desc) + tela/dropdown na `InvestimentosScreen` ou seção dedicada com listagem por mês (total do mês + ativos).
- Registro por voz (VOZ-03) tratado em 08-05; aqui apenas manual.

**Exclui:** voz (08-05), projeção, IR/composição de classes.

## Tasks
- [x] 08-04a: Domain `RendimentoInvestimento` + repos + providers (streams/por mês).
- [x] 08-04b: `RendimentoFormScreen` + seção rendimentos no detalhe do ativo.
- [x] 08-04c: Agregação por mês + `RendimentosScreen` em `/investimentos/rendimentos`.
- [x] 08-04d: Testes + `flutter analyze` + `flutter test` (175 testes verdes).

## Acceptance
1. Registrar rendimento por ativo/tipo/valor/data; editar e excluir.
2. Agregação por mês mostra total por mês e por ativo (mais recente primeiro).
3. `flutter analyze` limpo; testes verdes.