---
phase: 01-funda-o-e-acesso
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [PLAT-02]
must_haves:
  truths:
    - "Usuário consegue abrir o app no navegador (web) com a tela inicial do Meu Bolso"
    - "App é roda a partir de código único (Flutter) preparado para Android, iOS e Web"
    - "flutter analyze e flutter test passam sem erros"
  artifacts:
    - path: "app/pubspec.yaml"
      provides: "Manifesto Flutter com dependências riverpod/go_router/supabase"
      contains: "environment"
    - path: "app/lib/main.dart"
      provides: "Entrada do app com ProviderScope e MaterialApp tema financeiro"
      min_lines: 30
    - path: "app/test/widget_smoke_test.dart"
      provides: "Smoke test do fundo do app"
      min_lines: 15
    - path: "01-SKELETON.md"
      provides: "Contrato arquitetural para fases 2-8"
      contains: "Architectural Decisions"
---

<objective>
Criar o Walking Skeleton: ambiente Flutter instalado e configurado no Windows + projeto "Meu Bolso" inicializado em `app/` com tema financeiro PT-BR, home placeholder e smoke test — provando Android/iOS/Web a partir de um único código.

Purpose: Provar a fundação técnica (Flutter SDK + toolchain) e o esqueleto do app com build/lint/test verde. Toda fase posterior constrói sobre este contrato (SKELETON.md).
Output: Flutter instalado, `app/` criado e buildável, `01-SKELETON.md` definindo as decisões arquiteturais.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-funda-o-e-acesso/01-CONTEXT.md
@.planning/phases/01-funda-o-e-acesso/01-RESEARCH.md
@.planning/phases/01-funda-o-e-acesso/01-UI-SPEC.md
@.planning/phases/01-funda-o-e-acesso/01-SKELETON.md
</context>

<threat_model>
Threats addressed by this plan (ASVS L1):
- T-01-01: Ferramenta/código comprometido por instalação não-oficial → baixar SDK apenas de storages.googleapis.com / docs.flutter.dev; verificar `flutter doctor` sem sinais de origem não confiável.
- T-01-02: Aplicação sem tratamento de erro no boot (config/env ausente) → main.dart inicia Supabase com fallback claro se .env ausente; nunca expõe stack trace raw em produção.
- T-01-03: Dependências com supply chain → todas via pub.dev, versões pinned no pubspec.lock (commit do lock file).
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Instalar e verificar Flutter SDK no Windows</name>
  <files>app/</files>
  <read_first>.planning/phases/01-funda-o-e-acesso/01-RESEARCH.md</read_first>
  <action>
  Baixar o Flutter stable Windows atual (3.47.x, agosto/2026) do SDK archive oficial: https://storage.googleapis.com/flutter_infra_release/releases/stable/windows/flutter_windows_<version>-stable.zip (consultar https://docs.flutter.dev/install/archive para a versão exata vigente; usar a mais recente STABLE do canal Windows x64).

  Extrair para `C:\src\flutter` (criar pasta; nunca usar caminhos com espaço nem `C:\Program Files`). Se `C:\src\flutter` já existir, assumir instalação prévia e pular instalação.

  Adicionar `C:\src\flutter\bin` ao PATH do usuário via `setx PATH "$env:PATH;C:\src\flutter\bin"` (ou [Environment]::SetEnvironmentVariable persistente). Em seguida abrir novo processo para re-avaliar PATH: `$env:Path = "$env:Path;C:\src\flutter\bin"` no shell atual.

  Rodar `flutter --version` para confirmar, depois `flutter doctor -v` e `flutter doctor --android-licenses` (aceitar licenças com 'y').

  Tratar saídas:
  - Se "Android toolchain" falhar por falta de Android Studio/SDK → registrar pendência (user_setup) mas NÃO bloquear o plan: o alvo WEB e o build web continuam viáveis; anotar no resumo.
  - Se "Chrome" sumir → instalar via winget (`winget install Google.Chrome`) se necessário para `flutter run -d chrome`.
  - Qualquer erro de Flutter/dart propriamente → resolver antes de seguir.
  </action>
  <verify>flutter --version && flutter doctor</verify>
  <acceptance_criteria>
    - "flutter --version" retorna uma versão stable (ex.: `Flutter 3.47.x` com `Channel stable`)
    - "flutter doctor" lista `[✓] Flutter` e `[✓] Web (Chrome)` sem erros que bloqueiem build web
    - "flutter doctor --android-licenses" não acusa licenças pendentes (ou pendência documentada no resumo do plan)
    - "C:\src\flutter\bin" está presente no PATH persistente do usuário
  </acceptance_criteria>
  <done>Flutter stable instalado em C:\src\flutter; flutter doctor sem blocker no web; pendências Android/iOS anotadas</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold do app "Meu Bolso" em app/ com tema e home</name>
  <files>app/pubspec.yaml, app/lib/main.dart, app/lib/theme/app_theme.dart, app/lib/features/home/presentation/home_screen.dart, app/lib/features/home/domain/app_routes.dart, app/test/widget_smoke_test.dart</files>
  <read_first>app/pubspec.yaml, app/lib/main.dart, .planning/phases/01-funda-o-e-acesso/01-UI-SPEC.md</read_first>
  <action>
  Criar projeto na pasta `app/` (monorepo raiz): `flutter create --project-name meubolso --org br.com.meubolso --platforms android,ios,web app` (rodar de dentro da raiz do repo; se `app/` já existir com projeto válido, preservar e apenas ajustar).

  Substituir o código demo por:
  - `pubspec.yaml`: adicionar dependências `flutter_riverpod` (latest), `go_router` (latest) e `supabase_flutter` (latest estável) — rodar `flutter pub get`. MANTER `flutter_lints` do template. Confiar no pubspec.lock (commit).
  - `lib/main.dart`: `ProviderScope` na raiz; `MeuBolsoApp` com `MaterialApp(useMaterial3: true, title: 'Meu Bolso', locale: 'pt_BR', theme: AppTheme.light, darkTheme: AppTheme.dark)`. Inicializar `Supabase.initialize(url: String.fromEnvironment('SUPABASE_URL'), anonKey: String.fromEnvironment('SUPABASE_ANON_KEY'))` de forma tolerante: se vazio, logar aviso e seguir (auth entra no plan 01-03).
  - `lib/theme/app_theme.dart`: `ColorScheme.fromSeed(seedColor: Color(0xFF0B7A4B))`, `useMaterial3: true`, variantes light/dark conforme 01-UI-SPEC Design Tokens.
  - `lib/features/home/presentation/home_screen.dart`: tela Home placeholder segundo UI-SPEC S4 (AppBar "Meu Bolso", corpo centralizado "Fase 2 em breve", máx 480px no web).
  - `lib/features/home/domain/app_routes.dart`: constantes de rota (`/home`, `/login`, `/signup`) — go_router real entra no plan 01-03.
  - `test/widget_smoke_test.dart`: `flutter_test` — pump `MeuBolsoApp` e esperar o texto "Meu Bolso" aparecer (mock Supabase não necessário se inicialização tolerante).
  </action>
  <verify>cd app && flutter analyze && flutter test</verify>
  <acceptance_criteria>
    - "app/lib/main.dart" contém "ProviderScope" e "MeuBolsoApp"
    - "app/lib/theme/app_theme.dart" contém "0xFF0B7A4B" e "useMaterial3: true"
    - "app/lib/features/home/presentation/home_screen.dart" contém "Meu Bolso"
    - "app/pubspec.yaml" lista flutter_riverpod, go_router e supabase_flutter
    - "flutter analyze" sai com 0 issues
    - "flutter test" passa (smoke test verde)
  </acceptance_criteria>
  <done>App Flutter "Meu Bolso" scaffolded, buildável, lint+test verdes</done>
</task>

<task type="auto">
  <name>Task 3: Validar build web + registrar contrato arquitetural (SKELETON.md)</name>
  <files>app/.gitignore, 01-SKELETON.md, .gitignore, .github/</files>
  <read_first>.planning/phases/01-funda-o-e-acesso/01-SKELETON.md, .planning/phases/01-funda-o-e-acesso/01-RESEARCH.md</read_first>
  <action>
  1. Rodar `cd app && flutter build web --release` para provar o alvo web. Verificar que `build/web/index.html` foi gerado.
  2. Garantir que `.gitignore` (raiz e app/) ignora: `.env*`, `build/`, `*.dart_tool/` (Flutter gera). Confirmar que o app não versiona `.env` e nem `build/`.
  3. Registrar/buscar `.github/` de CI mínimo apenas se útil (opcional agora; deploy real é do plan 01-03).
  4. Preencher `01-SKELETON.md` (já criado no planejamento — confirmar que reflete as decisões EXECUTADAS: framework Flutter 3.47, arquitetura feature-first, Riverpod, go_router, Supabase sa-east-1, deploy GH Pages baseHref /projeto-financeiro/). Atualizar qualquer divergência real encontrada na execução.
  </action>
  <verify>cd app && flutter build web --release && git check-ignore app/.dart_tool</verify>
  <acceptance_criteria>
    - "app/build/web/index.html" existe após o build (build web green)
    - "git check-ignore" retorna non-zero path para `app/.env` (i.e., `.env` é ignorado) e para `.dart_tool`
    - "01-SKELETON.md" contém "## Architectural Decisions" e lista Stack Touched in Phase 1 completos
  </acceptance_criteria>
  <done>Build web verificado, .gitignore protege .env/build, SKELETON.md fiel ao executado</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd app && flutter analyze` — 0 issues
- [ ] `cd app && flutter test` — green
- [ ] `cd app && flutter build web --release` — build ok
- [ ] `.env*` e `build/` ignorados pelo git (check-ignore)
- [ ] `01-SKELETON.md` gravado com decisões reais
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Walking Skeleton entregue: app abre no web, código único preparado p/ Android/iOS/Web
</success_criteria>

<output>
After completion, create `.planning/phases/01-funda-o-e-acesso/01-01-SUMMARY.md`
</output>