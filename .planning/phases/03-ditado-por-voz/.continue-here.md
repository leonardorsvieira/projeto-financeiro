---
context: phase
phase: 03-ditado-por-voz
task: 0
total_tasks: 3
status: in_progress
last_updated: 2026-09-06T05:32:51.188Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> Nenhuma decisão relevante deve ser auto-decida — **perguntar ao usuário primeiro** (diretiva explícita do Leonardo: "volte a perguntar o que prefiro").

- [x] Usar **`gemini-2.5-flash`** e NUNCA `gemini-2.0-flash` (descontinuado em 01/06/2026 — verificado na pesquisa).
- [x] Consultar o usuário em decisões de UI/fluxo/deploy (padrão da Fase 2/3) em vez de escolher sozinho.

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Decidir sozinho | O usuário pediu explicitamente para voltar a perguntar preferências | blocking | Usar question tool para decisões de UX/fluxo/deploy; registrar em Decisões |
| Modelo descontinuado | gemini-2.0-flash não existe mais (Jun/2026) | blocking | Usar gemini-2.5-flash (audio + JSON) |
| Testes com rede/mic | Mic e HTTP não rodam em teste | advisory | fakes: fake_ditado_repository + fake_audio_recorder_service + http MockClient |

<current_state>
Fase 2 completa e validada (32 testes verdes, deploy 200, checkpoint manual OK pelo usuário; commits até 62cb104).
Fase 3 (Ditado por Voz) **planejada e commitada** em ee08d08, implementação **não iniciada**.
STATE.md: "Phase: 3 of 8 — Ditado por Voz (Core), Plan: 1 of 3, Status: Ready to execute".
</current_state>

<completed_work>

Fase 2 (integração — base da Fase 3):
- Schema Supabase `lancamentos` + RLS + realtime (migration aplicada, VERIFICADA via Management API).
- Camada de dados Dart (Lancamento centavos/BRL, converter, repository stream+CRUD, providers, fake repo).
- Telas lista (S5) + formulário (S6), router, 32 testes verdes, deploy GH Pages 200, checkpoint manual ✅.

Planejamento Fase 3 (commit ee08d08):
- 03-CONTEXT.md (D-31..D-38), 03-RESEARCH.md (gemini-2.5-flash; record webm; corrigir campo-a-campo via REST), 03-VALIDATION.md, 03-UI-SPEC.md (S7 ditado, S8 confirmação + 🎤 por campo, FAB Ditar), 03-SKELETON.md, 03-01/02/03-PLAN.md. ROADMAP waves e STATE atualizados.
</completed_work>

<remaining_work>

- **03-01 (wave 1)**: `flutter pub add record`; domain `RascunhoLancamento` + `DitadoRepository` (interface) + `corrigirCampo`; data `GeminiDitadoRepository` (REST `gemini-2.5-flash`, key via `String.fromEnvironment('GEMINI_API_KEY')`, `package:http`) + `gemini_prompt.dart` (JSON→RascunhoLancamento) + `AudioRecorderService` (record webm) + providers/controller; testes unitários (MockClient, fakes).
- **03-02 (wave 2)**: S7 `lancamento_ditado_screen.dart` (push-to-talk), S8 `confirmacao_ditado_screen.dart` (pré-preenchido, correção 🎤 campo-a-campo, merge), FAB expandido na lista (Ditar + Manual), rota `/lancamentos/ditar`, testes widget.
- **03-03 (wave 2)**: fluxo fim-a-fim (V1..V3), deploy com `GEMINI_API_KEY` (user fornece), checkpoint manual V10..V14, summaries, advance-plan x3, complete-phase, STATE/ROADMAP.
</remaining_work>

<decisions_made>

- D-31..D-38 (usuário): fluxo híbrido voz+botão; serviço único Gemini; confirmação editável antes de salvar; MVP = gasto simples; correção campo-a-campo via voz; valor ausente → campo vazio/foco; key Gemini do usuário; push-to-talk 1 frase.
- Transporte: Gemini REST `inline_data` (audio/webm base64) — não Live/websocket.
- Modelo: `gemini-2.5-flash`.
- Gravação: `record` (web MediaRecorder).
- Reuso Fase 2: converter BRL, listas categoria/forma, repositório lançamentos, validators.
</decisions_made>

<blockers>
- `GEMINI_API_KEY`: precisa do usuário (Google AI Studio free); só é necessária no plan 03-03 (deploy + teste real).
</blockers>

## Required Reading (in order)
1. `.planning/phases/03-ditado-por-voz/03-CONTEXT.md` — decisões do usuário (D-31..38)
2. `.planning/phases/03-ditado-por-voz/03-RESEARCH.md` — modelo/transporte/formatos; gemini-2.5-flash
3. `.planning/phases/03-ditado-por-voz/03-SKELETON.md` — arquivos novos/modificados
4. `.planning/phases/03-ditado-por-voz/03-01-PLAN.md` — próximo plano (iniciar por aqui)
5. `.planning/METHODOLOGY.md` (se existir)

## Critical Anti-Patterns (do NOT repeat these)
- Decidir sozinho → perguntar (question tool) antes de escolher UI/fluxo/deploy.
- Usar gemini-2.0-flash → sempre gemini-2.5-flash.
- Rodar mic/rede em testes → fakes e MockClient.
- JSON da IA em centavos → a IA devolve texto do valor (ex.: "40,00"); parse com `parseValorBRLParaCentavos` (defesa contra arredondamento).

## Infrastructure State
- Flutter em `C:\src\flutter\bin` (prepend no PATH de cada sessão antes de `flutter ...`).
- Supabase ref `tkfhthotspehsgvmpsjm` (sa-east-1); migration lancamentos já aplicada; PAT atual `sbp_c0e...` (só via env; anterior revogado).
- GH Pages deploy automático no push para main (workflow `deploy-pages`; hoje `--dart-define` SUPABASE_URL/ANON_KEY; 03-03 adiciona GEMINI_API_KEY secret).
- Commits: junção da fase 3 planejada em ee08d08; nada da Fase 3 implementado.

## Pre-Execution Critique Required
- Design artifact: `.planning/phases/03-ditado-por-voz/03-01-PLAN.md` (e UI-SPEC para 03-02).
- Critique focus: prompt/schema do Gemini (campos null, valores BRL via texto), robustez do AudioRecorderService em web, e se o `record` realmente suporta `webmOpus` + como os bytes chegam para base64.
- Gate: OK iniciar 03-01; antes de 03-02 confirmar UI-SPEC com o usuário? (não necessário — já acordado na discussão).

<context>
Estávamos na transição Fase 2→3: discussão concluída (D-31..38 todas respondidas pelo usuário), pesquisa feita (modelo 2.5-flash), planning commitado em ee08d08. Próximo passo natural: executar plan 03-01. O usuário pediu para parar ("vamos parar por agora, depois continuamos").
</context>

<next_action>
Ao retomar: `gsd-execute-phase` para o plan 03-01 — começar por `flutter pub add record` no workdir `app/`, construção de `lib/features/ditado/` conforme 03-SKELETON.md, e os testes unitários do `GeminiDitadoRepository` com `MockClient`. Consultar o usuário se aparecer decisão que mude UX, modelo de dados ou deploy.
</next_action>